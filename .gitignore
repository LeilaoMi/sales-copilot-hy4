# 运行时数据：真实客户、商机、跟进记录，含姓名 / 电话 / 微信。
# 这是销售最核心的资产，也是别人的隐私，永远不许进版本库。
# 就算仓库设成私有也一样 —— 私有仓库照样会被误共享、误导出、误截图。
data/

# 依赖
node_modules/

# 日志与临时文件
*.log
*.tmp
.DS_Store
Thumbs.db

# 编辑器 / 系统
.vscode/
.idea/
*.swp

# ────────────────────────────────────────────────────────────
# 部署相关的补充（原有规则一律保留，只在后面追加）
# ────────────────────────────────────────────────────────────

# 环境变量：本地调试时可能会往里塞 Supabase 的 service_role key
# 或 AI 接口的 API key。这些一旦提交就永远是仓库历史的一部分，
# 就算后面删掉文件，历史里的那份照样能被翻出来。
.env
.env.*
!.env.example

# 平台 CLI 的本地产物，带机器路径和部署 ID，跟别人机器上一定冲突
.vercel/
.wrangler/
.netlify/

# public/ 整个是构建产物：node build.js --pages 生成，每次先清空。
# 里面所有文件都是别处的副本，提交进去只会跟源文件抢着改。
# 缓存规则 _headers 的源文件在 deploy/_headers，由构建脚本复制过去。
public/
